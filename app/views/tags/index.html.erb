<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      

      <%= link_to article.title, tag_path(tag) %>



      <%= link_to "Create a New Tag", new_tag_path, class: "new_tag" %>
    </li>
  <% end %>
</ul>


<%= link_to "delete", tag_path(@tag) , 
        method: :delete , 
     data: {confirm: "Really delete the tag?"}%>